fields:
  - name: amount
    logical_type:
      name: decimal
      args:
        as_float: true
        precision: 5
        scale: 2
    representations:
      - pattern: "(?P<value>[0-9]+\\.[0-9]+)"
